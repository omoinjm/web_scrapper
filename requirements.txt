# Web Scraper & Archiver — Python dependencies
# Install with: pip install -r requirements.txt
# Then run: playwright install chromium

# Async HTTP client
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.3
lxml>=5.2.1           # faster BS4 parser (optional but recommended)

# Headless browser / PDF rendering
playwright>=1.44.0
